{
  "buildCommand": "PORT=3000 BASE_PATH=/ pnpm run build && ls -la artifacts/summarizer/dist",
  "outputDirectory": "artifacts/summarizer/dist",
  "installCommand": "pnpm install",
  "env": {
    "PORT": "3000",
    "BASE_PATH": "/"
  }
}
